{
    "extends": "./tsconfig.json",
    "exclude": ["tests/**/*", "tools/**/*", "typings/**/*", "benchmark/**/*"],
    "compilerOptions": {
        "removeComments": true /* Do not emit comments to output. */
    }
}
